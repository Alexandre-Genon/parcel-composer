import { CustomerOnMap } from './customer-on-map';

describe('CustomerOnMap', () => {
  it('should create an instance', () => {
    expect(new CustomerOnMap(undefined, undefined, undefined)).toBeTruthy();
  });
});
